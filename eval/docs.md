# Eval document set

The PDFs themselves live in `eval/docs/` (gitignored — they may be large or
non-redistributable). This file is the reproducible record of what belongs in
the set. See [docs/eval/retrieval-eval.md](../docs/eval/retrieval-eval.md)
for the protocol.

| filename | source / provenance | why it's in the set |
|---|---|---|
| example.pdf | REPLACE ME (URL or internal origin) | e.g. dense tables, two-column layout |
